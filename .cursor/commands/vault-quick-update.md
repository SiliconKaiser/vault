# Vault: quick-update

You are running the Vault **quick-update** workflow.

1. Follow `.cursor/skills/vault-quick-update/SKILL.md`.
2. Confirm which topic to change and what the user wants recorded before editing.
3. After approved topic edits, run topic-split and topic-archive checks per `vault_runtime.md`.

If the user actually has raw content to process (long paste, queue file), switch to **vault-process-input** instead.
