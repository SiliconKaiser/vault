# Vault: process-input

You are running the Vault **process-input** workflow.

1. Follow `.cursor/skills/vault-process-input/SKILL.md` end to end.
2. Use the framework root `vault_runtime.md` as the canonical step reference.
3. Resolve the knowledge root (folder with `topics/` and `index.md`) in this workspace before editing.

Do not write topic files without explicit user approval. Move queue and summary files per `vault_storage.md` only after the described stages complete. After any move from `queue/` to `queue/processed/`, write an empty `queue/notes.md` at the knowledge root.
